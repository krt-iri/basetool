# Prettier formats only our hand-written CSS/JS (see the prettier* Gradle tasks).
# Vendored, minified third-party bundles and build output are never reformatted.
build/
node_modules/
src/main/resources/static/js/vendor/
