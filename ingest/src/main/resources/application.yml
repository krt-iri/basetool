server:
  port: 11262
  # Behind nginx-proxy-manager (TLS terminated at the proxy). `native` routes X-Forwarded-* through
  # Tomcat's RemoteIpValve, which only honours the header from a TRUSTED internal proxy (its default
  # internal-proxies regex covers the Docker private ranges, including the NPM hop). Unlike
  # `framework`, this stops an external client spoofing an arbitrary X-Forwarded-For to mint a fresh
  # per-IP rate-limit budget (security audit INGEST-RATELIMIT-1). The enforceable throttle is the
  # per-subject limiter; this only hardens the coarse per-IP front line.
  forward-headers-strategy: native
  max-http-request-header-size: 65536
  shutdown: graceful
  ssl:
    # The gateway serves PLAIN HTTP — nginx-proxy-manager terminates TLS and proxies to this port
    # over http://. The SERVER_SSL_KEY_STORE / SERVER_SSL_KEY_STORE_PASSWORD env vars are passed to
    # this module ONLY to feed the `backend-trust` SSL *bundle* (the truststore that validates the
    # backend's self-signed https://backend:11261 cert — see application-prod.yml). Spring, however,
    # also binds `server.ssl.key-store` from that same env var, which would switch the embedded
    # Tomcat connector to HTTPS on 11262 — breaking NPM's plain-HTTP proxy (→ bare 400) and the
    # Docker HEALTHCHECK (wget http://localhost:11262/... → container stays unhealthy). Forcing the
    # server connector off here keeps the gateway on HTTP regardless of the keystore env vars; the
    # `spring.ssl.bundle.*` truststore is independent of this flag and stays available.
    enabled: false

spring:
  application:
    name: ingest
  threads:
    virtual:
      enabled: true
  mvc:
    problemdetails:
      enabled: true
  messages:
    fallback-to-system-locale: false
  web:
    error:
      whitelabel:
        enabled: false
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:https://keycloak.profit-base.online/realms/iri}

app:
  ingest:
    backend-base-url: ${BACKEND_URL:https://backend:11261}
    frontend-base-url: ${FRONTEND_URL:https://app.profit-base.online}
    refinery-path: /refinery-orders/create
    blueprint-path: /personal-inventory/blueprints
    handoff-ttl: PT5M
    max-payload-bytes: 2097152
  rate-limit:
    enabled: true
    capacity: 30
    refill-tokens: 30
    refill-period: PT1M
  security:
    jwt:
      # Opt-in audience check. Empty by default; set to `basetool-backend` ONLY after both the
      # extractor and frontend tokens carry that audience (docs/INGEST_KEYCLOAK_SETUP.md).
      expected-audiences: ${APP_SECURITY_JWT_EXPECTED_AUDIENCES:}

# Resilience4j around the backend relay. HTTP response errors are ignored (a 400 envelope reject is
# a client problem, not a backend-health signal); only transport failures open the breaker.
resilience4j:
  circuitbreaker:
    instances:
      backend:
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 60
        wait-duration-in-open-state: 10s
        ignore-exceptions:
          - org.springframework.web.reactive.function.client.WebClientResponseException

logging:
  level:
    root: INFO
    io.github.resilience4j: INFO

# Actuator: expose only `health` (Docker HEALTHCHECK / compose service_healthy gating); bare status.
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
      show-components: never
      probes:
        enabled: true
