<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging for the ingest gateway. Plain console in dev/test; structured JSON (LogstashEncoder) in
  prod so the correlation id and other MDC fields are queryable. NEVER log tokens, names or emails
  (REQ-OBS-*): the application code only ever logs the draft kind and the correlation id.
-->
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <springProfile name="prod">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeMdcKeyName>correlationId</includeMdcKeyName>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON"/>
    </root>
  </springProfile>

  <springProfile name="!prod">
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>
</configuration>
